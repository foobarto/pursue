name: lint

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  frontmatter:
    name: Skill frontmatter
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5.6.0
        with:
          python-version: "3.12"
      - name: Install PyYAML
        run: pip install --quiet pyyaml
      - name: Validate SKILL.md
        run: python3 scripts/validate_skill.py

  shellcheck:
    name: ShellCheck
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
      - name: Run shellcheck
        uses: ludeeus/action-shellcheck@00cae500b08a931fb5698e11e79bfbd38e612a38 # 2.0.0
        with:
          scandir: "."
          check_together: "yes"
          # Pinned so a local run is a faithful preflight. The action itself
          # is SHA-pinned, but it downloads the shellcheck binary at runtime,
          # so an unpinned `stable` lets an upstream release fail CI with no
          # change to this repo -- which is exactly what happened when 0.9.0
          # passed locally and a newer build failed here on SC2329.
          version: "v0.9.0"
        env:
          # Match the standard the branch holds itself to locally:
          # `shellcheck -x` at shellcheck's own default severity. Without -x
          # the sourced hooks/lib/common.sh is never followed, and a
          # `severity: warning` floor hides info- and style-level findings
          # that a bare local run would report.
          SHELLCHECK_OPTS: -x

  install-script:
    name: install.sh (bats)
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5.6.0
        with:
          python-version: "3.12"
      - name: Install bats
        run: |
          sudo apt-get update
          sudo apt-get install -y bats
      - name: Run bats tests
        run: bats tests/
